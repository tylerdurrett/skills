---
name: explainer-video
description: Create a 30-60s whimsical hand-drawn collage-style explainer video in pure JavaScript animation, with high-quality TTS narration and audio, rendered as a 4K mp4. Use when the user wants an explainer video, animated explainer, or narrated animation on a topic.
---

Create a pure javascript animation explainer video. 30s-60s whimsical hand drawn collage style with appropriate audio on the topic. If the user provides style overrides, take the guidance.

Before beginning, align with the user on what the topic is and arrive at a basic outline. Once you are aligned and the user says to proceed, you'll create the video.

Orchestrate sub agents as needed so you preserve your context.

Entire video should be as high of a production value as possible. Please spend your time on this, it's very important.

Use high quality text-to-speech model for generation. You can find open router API key in .env file

You can use any tools you can find access to and resources on the internet. You create the script, the assets, the animation, concept, everything.
I have to go away from my computer so please work autonomously until done. Quality is paramount. Production value should be on professional level.

One more thing: max OpenRouter spend is $10

When done, render video as a 4K high quality mp4 and save it locally on this machine.

Keep all project files and the final mp4 in `outputs/<video-slug>/` at the repo root. `outputs/` must be gitignored; add it to `.gitignore` if it isn't.
